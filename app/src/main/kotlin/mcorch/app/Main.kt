package mcorch.app

/**
 * Entry point. Wiring only — the reconcile loop, store, node, and API server
 * are not stood up yet.
 */
public fun main() {
    println("mc-server-orchestrator: scaffold only — nothing is reconciling yet.")
}
