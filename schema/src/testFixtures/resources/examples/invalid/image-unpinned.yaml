# Expected: spec.image — must be pinned to a tag or a digest
apiVersion: mcorch.dev/v1alpha1
kind: PaperServer
metadata:
  name: survival-01
spec:
  network:
    rcon:
      passwordSecret:
        name: survival-01-rcon
        key: password
  eulaAccepted: true
  image: docker.io/itzg/minecraft-server
  paper:
    minecraftVersion: "1.21.8"
  resources:
    memory: 4Gi
